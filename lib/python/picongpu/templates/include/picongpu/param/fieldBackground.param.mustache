/* Copyright 2014-2024 Axel Huebl, Alexander Debus, Richard Pausch
 *
 * This file is part of PIConGPU.
 *
 * PIConGPU is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 *
 * PIConGPU is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
 * GNU General Public License for more details.
 *
 * You should have received a copy of the GNU General Public License
 * along with PIConGPU.
 * If not, see <http://www.gnu.org/licenses/>.
 */

/** @file fieldBackground.param
 *
 * Load external background fields
 */

#pragma once

namespace picongpu
{
{{^background_field}}
    class FieldBackgroundE
    {
    public:
        /* Add this additional field for pushing particles */
        static constexpr bool InfluenceParticlePusher = false;

        /* We use this to calculate your SI input back to our unit system */
        PMACC_ALIGN(m_unitField, float3_64 const);

        HINLINE FieldBackgroundE(float3_64 const unitField) : m_unitField(unitField)
        {
        }

        HDINLINE FieldBackgroundE(FieldBackgroundE const&) = default;

        /** Specify your background field E(r,t) here
         *
         * @param cellIdx The total cell id counted from the start at t = 0
         * @param currentStep The current time step */
        HDINLINE float3_X operator()(DataSpace<simDim> const& cellIdx, uint32_t const currentStep) const
        {
            /* example: periodicity of 20 microns ( = 2.0e-5 m) */
            constexpr float_64 period_SI(20.0e-6);
            /* calculate cells -> SI [m] */
            float_64 const y_SI = cellIdx.y() * sim.si.getCellSize().y();
            /* note: you can also transform the time step to seconds by
             *       multiplying with DELTA_T_SI */

            /* specify your E-Field in V/m and convert to PIConGPU units */
            float_X const sinArg = precisionCast<float_X>(y_SI / period_SI * 2.0 * PI);
            return float3_X(0.0, math::sin(sinArg) / m_unitField[1], 0.0);
        }
    };

    class FieldBackgroundB
    {
    public:
        /* Add this additional field for pushing particles */
        static constexpr bool InfluenceParticlePusher = false;

        /* We use this to calculate your SI input back to our unit system */
        PMACC_ALIGN(m_unitField, float3_64 const);

        HINLINE FieldBackgroundB(float3_64 const unitField) : m_unitField(unitField)
        {
        }

        HDINLINE FieldBackgroundB(FieldBackgroundB const&) = default;

        /** Specify your background field B(r,t) here
         *
         * @param cellIdx The total cell id counted from the start at t=0
         * @param currentStep The current time step */
        HDINLINE float3_X operator()(DataSpace<simDim> const& cellIdx, uint32_t const currentStep) const
        {
            /* example: periodicity of 20 microns ( = 2.0e-5 m) */
            constexpr float_64 period_SI(20.0e-6);
            /* calculate cells -> SI -> [m] */
            float_64 const y_SI = cellIdx.y() * sim.si.getCellSize().y();
            /* note: you can also transform the time step to seconds by
             *       multiplying with DELTA_T_SI */

            /* specify your B-Field in T and convert to PIConGPU units */
            float_X const sinArg = precisionCast<float_X>(y_SI / period_SI * 2.0 * PI);
            return float3_X(0.0, math::cos(sinArg) / m_unitField[1], 0.0);
        }
    };
{{/background_field}}

{{#background_field}}
    /* The following background field functors are generated by the
     * Python/PICMI layer (see BackgroundField).
     *
     * The field expressions are evaluated on the whole simulation domain and
     * use SI quantities only: x, y, z are the position in m, t the time in s.
     * The expressions for E are in V/m and for B in T; both are divided by the
     * corresponding field unit to convert into PIConGPU internal units. */
    class FieldBackgroundE
    {
    public:
        /* Add this additional field for pushing particles */
        static constexpr bool InfluenceParticlePusher = true;

        /* We use this to calculate your SI input back to our unit system */
        PMACC_ALIGN(m_unitField, float3_64 const);

        HINLINE FieldBackgroundE(float3_64 const unitField) : m_unitField(unitField)
        {
        }

        HDINLINE FieldBackgroundE(FieldBackgroundE const&) = default;

        /** Specify your background field E(r,t) here
         *
         * @param cellIdx The total cell id counted from the start at t = 0
         * @param currentStep The current time step */
        HDINLINE float3_X operator()(DataSpace<simDim> const& cellIdx, uint32_t const currentStep) const
        {
            {{#user_defined_kw}}
            constexpr float_64 {{{name}}} = {{{value}}};
            {{/user_defined_kw}}
            /* calculate cells -> SI [m] and steps -> SI [s] */
            [[maybe_unused]] float_64 const x = cellIdx.x() * sim.si.getCellSize().x();
            [[maybe_unused]] float_64 const y = cellIdx.y() * sim.si.getCellSize().y();
            [[maybe_unused]] float_64 const z = cellIdx.z() * sim.si.getCellSize().z();
            [[maybe_unused]] float_64 const t = currentStep * sim.si.getDt();

            /* specify your E-Field in V/m and convert to PIConGPU units */
            return float3_X(
                precisionCast<float_X>(({{{ex}}}) / m_unitField[0]),
                precisionCast<float_X>(({{{ey}}}) / m_unitField[1]),
                precisionCast<float_X>(({{{ez}}}) / m_unitField[2]));
        }
    };

    class FieldBackgroundB
    {
    public:
        /* Add this additional field for pushing particles */
        static constexpr bool InfluenceParticlePusher = true;

        /* We use this to calculate your SI input back to our unit system */
        PMACC_ALIGN(m_unitField, float3_64 const);

        HINLINE FieldBackgroundB(float3_64 const unitField) : m_unitField(unitField)
        {
        }

        HDINLINE FieldBackgroundB(FieldBackgroundB const&) = default;

        /** Specify your background field B(r,t) here
         *
         * @param cellIdx The total cell id counted from the start at t=0
         * @param currentStep The current time step */
        HDINLINE float3_X operator()(DataSpace<simDim> const& cellIdx, uint32_t const currentStep) const
        {
            {{#user_defined_kw}}
            constexpr float_64 {{{name}}} = {{{value}}};
            {{/user_defined_kw}}
            /* calculate cells -> SI [m] and steps -> SI [s] */
            [[maybe_unused]] float_64 const x = cellIdx.x() * sim.si.getCellSize().x();
            [[maybe_unused]] float_64 const y = cellIdx.y() * sim.si.getCellSize().y();
            [[maybe_unused]] float_64 const z = cellIdx.z() * sim.si.getCellSize().z();
            [[maybe_unused]] float_64 const t = currentStep * sim.si.getDt();

            /* specify your B-Field in T and convert to PIConGPU units */
            return float3_X(
                precisionCast<float_X>(({{{bx}}}) / m_unitField[0]),
                precisionCast<float_X>(({{{by}}}) / m_unitField[1]),
                precisionCast<float_X>(({{{bz}}}) / m_unitField[2]));
        }
    };
{{/background_field}}

    class FieldBackgroundJ
    {
    public:
        /* Add this additional field? */
        static constexpr bool activated = false;

        /* We use this to calculate your SI input back to our unit system */
        PMACC_ALIGN(m_unitField, float3_64 const);

        HINLINE FieldBackgroundJ(float3_64 const unitField) : m_unitField(unitField)
        {
        }

        HDINLINE FieldBackgroundJ(FieldBackgroundJ const&) = default;

        /** Specify your background field J(r,t) here
         *
         * @param cellIdx The total cell id counted from the start at t=0
         * @param currentStep The current time step */
        HDINLINE float3_X operator()(DataSpace<simDim> const& cellIdx, uint32_t const currentStep) const
        {
            /* example: periodicity of 20 microns ( = 2.0e-5 m) */
            constexpr float_64 period_SI(20.0e-6);
            /* calculate cells -> SI -> m to microns*/
            float_64 const y_SI = cellIdx.y() * sim.si.getCellSize().y() * 1.0e6;
            /* note: you can also transform the time step to seconds by
             *       multiplying with DELTA_T_SI */

            /* specify your J-Field in A/m^2 and convert to PIConGPU units */
            float_X const sinArg = precisionCast<float_X>(y_SI / period_SI * 2.0 * PI);
            return float3_X(0.0, math::cos(sinArg) / m_unitField[1], 0.0);
        }
    };

} // namespace picongpu
